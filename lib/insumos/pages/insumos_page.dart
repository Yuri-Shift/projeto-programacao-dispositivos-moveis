import 'package:flutter/material.dart';

class InsumosPage extends StatelessWidget {
  const InsumosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 250,
      height: 250,
      color: Colors.yellowAccent,
      child: const Center(child: Text('Insumos')),
    );
  }
}
